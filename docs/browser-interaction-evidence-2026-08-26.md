Final browser evidence 2026-08-26: The contenteditable Discord composer with aria-label `Message #general` is focused and currently contains plain text `/test/`. The native slash popup was visible earlier and showed `/start position`, but browser automation did not select and submit the command form. REST checks confirmed bot user ID equals configured application ID and the guild currently has 53 registered commands. No credentials are recorded here.
